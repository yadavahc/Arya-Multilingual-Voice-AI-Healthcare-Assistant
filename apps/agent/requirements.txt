# LiveKit Agents (voice orchestration) + plugins
livekit-agents[openai,silero,turn-detector]~=1.0
livekit-plugins-openai~=1.0
livekit-plugins-silero~=1.0
# Sarvam AI — Indian-language STT + TTS (Hindi, Kannada, Tamil, English)
livekit-plugins-sarvam~=1.0
# Fallback pipeline (optional)
livekit-plugins-deepgram~=1.0
livekit-plugins-cartesia~=1.0

# Session state + context cache
redis>=5.0
upstash-redis>=1.2

# HTTP to FastAPI service + Firebase
httpx>=0.27
python-dotenv>=1.0

# Observability
sentry-sdk>=2.0
